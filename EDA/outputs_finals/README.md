# Finals-Filtered Datasets

Generated by `EDA/13_build_finals_datasets.py`.

**Source (unchanged midterm base):** `EDA/outputs/`
**This folder:** post-presentation clean analysis pool.

## Filter layers

### Layer 1 — DQ drops (order-level)
| ID | Rule | Dropped |
|----|------|---------|
| DQ-02 | Zero revenue + zero discount | 336 orders |
| DQ-03 | 100%-discount free fulfilments | 1,281 orders |
| DQ-04 | wholesale-sale tag OR > S$5,000 | 78 orders |
| **Retained** | | **25,658 orders** |

### Layer 2 — LP feedback (customer-level → `finals_eligible`)
| ID | Rule | Flagged |
|----|------|---------|
| LP-F01 | Exclude `better-whey-protein-elite` buyers | 167 |
| LP-F02 | Exclude Jul/Nov acquisitions | 2,818 |
| LP-F03 | first_order_date >= 2022-01-01 | 5,016 pre-2022 |
| LP-F04 | First order 51%+ discount | 480 |
| **Finals-eligible** | all four LP filters pass | **6,353 customers** |

### Layer 3 — SKU analysis (`lines_sku_analysis.parquet`)
- Finals-eligible customers
- Orders from 2022-01-01 onwards
- Exclude July & November **order months**
- Exclude `better-whey-protein-elite` line items

## Files

| File | Description | Rows |
|------|-------------|------|
| `orders.parquet` | Primary — finals cohort orders (L1+L2) | 9,852 |
| `lines.parquet` | Primary — finals cohort line items (L1+L2) | 16,040 |
| `customers.parquet` | Primary — finals-eligible customers with flags | 6,353 |
| `lines_sku_analysis.parquet` | SKU analysis subset (L1+L2+L3) | 14,448 |
| `orders_dq_clean.parquet` | Reference — DQ-clean only (all customers) | 25,658 |
| `lines_dq_clean.parquet` | Reference — DQ-clean lines | 46,280 |
| `customers_dq_clean.parquet` | Reference — DQ-clean customers + filter flags | 13,780 |
| `products.parquet` | Product master (+ `is_excluded_elite` flag) | 167 |
| `discounts.parquet` | Discount codes (standalone reference) | 367 |
| `rc_*.parquet` | Recharge data filtered to finals cohort | see manifest.json |

## Usage

```python
import pandas as pd
from pathlib import Path
FINALS = Path("EDA/outputs_finals")

orders = pd.read_parquet(FINALS / "orders.parquet")
customers = pd.read_parquet(FINALS / "customers.parquet")
lines_sku = pd.read_parquet(FINALS / "lines_sku_analysis.parquet")
```

Re-run after any filter change:
```bash
python EDA/13_build_finals_datasets.py
```
