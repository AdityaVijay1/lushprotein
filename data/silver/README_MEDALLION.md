# Legacy path — Medallion migration

**Silver layer data has moved to `data/silver/`.**

This directory is a **junction** to the canonical Silver layer.  
See **`data/README.md`** and **`data/silver/README.md`**.

```bash
python data/pipeline/run_bronze_to_silver.py
```
